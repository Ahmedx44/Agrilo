import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFD3FC4F); // The lime color from image
  static const Color background = Color(0xFF0F0F0F); // Dark background
  static const Color cardBackground = Color(0xFF1C1C1E);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
  static const Color error = Colors.redAccent;
  
  // Light theme colors
  static const Color lightBackground = Color(0xFFF5F5F7);
  static const Color lightCardBackground = Colors.white;
  static const Color lightTextPrimary = Color(0xFF1D1D1F);
  static const Color lightTextSecondary = Color(0xFF86868B);
}
